package com.rangerscards.data.objects

import com.rangerscards.R

object DeckErrorsMap {
    val deckErrorsMap by lazy {
        mapOf(
            "invalid_aspects" to R.string.no_decks,
        )
    }
}